local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")

local ScreenGui = Instance.new("ScreenGui")
local OpenButton = Instance.new("TextButton")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local CloseButton = Instance.new("TextButton")
local Tab1 = Instance.new("TextButton")
local Content = Instance.new("Frame")
local ExecuteButton = Instance.new("TextButton")

ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

-- زر الفتح
OpenButton.Parent = ScreenGui
OpenButton.Size = UDim2.new(0,60,0,60)
OpenButton.Position = UDim2.new(0,20,0.5,-30)
OpenButton.Text = "☰"
OpenButton.BackgroundColor3 = Color3.fromRGB(30,30,30)
OpenButton.TextColor3 = Color3.new(1,1,1)
OpenButton.TextScaled = true

-- القائمة
MainFrame.Parent = ScreenGui
MainFrame.Size = UDim2.new(0,400,0,250)
-- start off-screen (we tween into view)
local openPos = UDim2.new(0.5,-200,0.5,-125)
local closedPos = UDim2.new(0.5,-200,-0.8,-125)
MainFrame.Position = closedPos
MainFrame.BackgroundColor3 = Color3.fromRGB(25,25,25)
MainFrame.Visible = false -- ✅ إخفاء في البداية

-- العنوان
Title.Parent = MainFrame
Title.Size = UDim2.new(1,0,0,35)
Title.BackgroundColor3 = Color3.fromRGB(35,35,35)
Title.Text = "Delta Hub"
Title.TextColor3 = Color3.new(1,1,1)
Title.TextScaled = true

-- زر الإغلاق
CloseButton.Parent = Title
CloseButton.Size = UDim2.new(0,35,0,35)
CloseButton.Position = UDim2.new(1,-35,0,0)
CloseButton.Text = "✕"
CloseButton.BackgroundColor3 = Color3.fromRGB(255,0,0)
CloseButton.TextColor3 = Color3.new(1,1,1)
CloseButton.TextScaled = true

-- التاب
Tab1.Parent = MainFrame
Tab1.Size = UDim2.new(0,100,0,30)
Tab1.Position = UDim2.new(0,5,0,40)
Tab1.Text = "Main"
Tab1.BackgroundColor3 = Color3.fromRGB(45,45,45)
Tab1.TextColor3 = Color3.new(1,1,1)

-- المحتوى
Content.Parent = MainFrame
Content.Size = UDim2.new(1,-10,1,-80)
Content.Position = UDim2.new(0,5,0,75)
Content.BackgroundTransparency = 1

-- زر التشغيل
ExecuteButton.Parent = Content
ExecuteButton.Size = UDim2.new(0,180,0,40)
ExecuteButton.Position = UDim2.new(0,10,0,10)
ExecuteButton.Text = "Run Script"
ExecuteButton.BackgroundColor3 = Color3.fromRGB(0,170,255)
ExecuteButton.TextColor3 = Color3.new(1,1,1)
ExecuteButton.TextScaled = true

-- helper to open/close with tween
local tweenInfo = TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
local function setOpen(state)
    if state then
        MainFrame.Visible = true
        local t = TweenService:Create(MainFrame, tweenInfo, {Position = openPos})
        t:Play()
    else
        local t = TweenService:Create(MainFrame, tweenInfo, {Position = closedPos})
        t:Play()
        t.Completed:Connect(function()
            MainFrame.Visible = false
        end)
    end
end

-- safer remote execution: pcall around HttpGet + loadstring
ExecuteButton.MouseButton1Click:Connect(function()
    ExecuteButton.Text = "Running..."
    ExecuteButton.Active = false
    -- Use the raw URL for the script. If your raw file has an extension or different path, adjust accordingly.
    local rawUrl = "https://raw.githubusercontent.com/gemzgaze-hash/Script-bang-/main/Bang%20script"
    local ok, result = pcall(function()
        -- game:HttpGet can fail if HTTP is disabled in the environment; pcall will catch that
        local body = game:HttpGet(rawUrl)
        -- ensure we actually got text
        if not body or #body == 0 then
            error("Remote script is empty or not found at: " .. rawUrl)
        end
        local f = loadstring(body)
        if type(f) ~= "function" then
            error("Loaded remote content is not a function.")
        end
        -- execute the loaded function safely
        f()
    end)

    if ok then
        ExecuteButton.Text = "Done"
        task.delay(1.2, function()
            if ExecuteButton and ExecuteButton.Parent then
                ExecuteButton.Text = "Run Script"
            end
        end)
    else
        -- print and give basic feedback; avoid showing raw errors to users if desired
        warn("Failed to execute remote script:", result)
        ExecuteButton.Text = "Run Script"
    end
    ExecuteButton.Active = true
end)

-- فتح وإغلاق (toggle now uses tween)
OpenButton.MouseButton1Click:Connect(function()
    setOpen(not MainFrame.Visible)
end)

CloseButton.MouseButton1Click:Connect(function()
    setOpen(false)
end)

-- سحب القائمة (محسّن)
local dragging = false
local dragStart
local startPos

-- Enable dragging by title (works with mouse)
Title.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

Title.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

UIS.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

-- keyboard: close on ESC
UIS.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == Enum.KeyCode.Escape then
        setOpen(false)
    end
end)
